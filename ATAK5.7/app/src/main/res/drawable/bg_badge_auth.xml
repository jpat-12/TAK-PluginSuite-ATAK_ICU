<?xml version="1.0" encoding="utf-8"?>
<!-- Purple pill for the "Using Auth" badge in the home header. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/icu_auth_dim" />
    <stroke android:width="1dp" android:color="@color/icu_auth" />
    <corners android:radius="12dp" />
</shape>
